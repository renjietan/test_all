enum IconTextAlignment {
  iconTopTextBottom, //图在上文字在下
  iconBottomTextTop, //图在下文字在上
  iconLeftTextRight, //图在左文字在右
  iconRightTextLeft, //图在右文字在左
}

enum IconTextType {
  ellipse, // 椭圆
  rounded_rectangle, // 圆角
}

enum SimpleButtonType {
  ellipse, // 椭圆
  rounded_rectangle, // 圆角
}

enum SimpleCounterButtonType {
  circle, // 圆形
  rectangle, //矩形
}

enum SimpleNotifyType {
  success,
  warn,
  error,
  info,
}
