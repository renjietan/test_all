import 'package:flutter/material.dart';

class ButtonConfig {
  /// 按钮背景色
  static final Color backgroundColor = Color.fromRGBO(62, 103, 255, 1);

  /// 按钮文本颜色
  static final Color textColor = Colors.white;

  /// 按钮按下时颜色
  static final Color highlightColor = Color.fromRGBO(62, 103, 255, 1);

  /// 按钮水波纹颜色
  static final Color splashColor = Color.fromRGBO(62, 88, 88, 1);

  /// 按钮禁用颜色
  static final Color disabledColor = Color.fromRGBO(62, 103, 255, 0.6);

  ///按钮禁用 文本色
  static final Color disabledTextColor = Colors.white;

  /// 按钮高度
  static final double height = 45.0;

  /// 按钮线框宽度
  static final double borderWidth = 1.0;

  /// 按钮线框颜色
  static final Color borderColor = Colors.transparent;

  /// 投影
  static final double elevation = 1.0;

  /// 圆角
  static final double radius = 8.0;

  /// 字体大小
  static final double fontSize = 12.0;
}
